<%# 1. tasks_container の先頭に、新しく作ったタスクを表示する %>
<%= turbo_stream.prepend "tasks_container", partial: "tasks/task", locals: { task: @task } %>

<%# 2. フォームの中身を空にして、次の入力に備える %>
<%= turbo_stream.replace "new_task_form_container" do %>
  <%= render "form_quick_input", task: Task.new %>
<% end %>